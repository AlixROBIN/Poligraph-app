apiVersion: v1
kind: Secret
metadata:
  name: poligraph-secrets
  namespace: poligraph
type: Opaque
stringData:
  GROQ_API_KEY: ""          # Rempli automatiquement par deploy.ps1 depuis .env
  LLM_BACKEND: "groq"       # "groq" en k8s (Ollama = local uniquement)
  GROQ_MODEL: "llama-3.1-8b-instant"
